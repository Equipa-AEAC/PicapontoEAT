Theme

Dark

Radius

10px

Spacing

8px Grid

Icons

Phosphor

Cards

Rounded

Animations

Minimal

Desktop First

Responsive

Enterprise Dashboard

Minimalist

One title per page — the page header owns it. No eyebrow, no breadcrumbs, no duplicate title
in the topbar.

One label per selectable item — sidebar entries and menu items carry a label only, never a
second description line.

Content lives in cards — every base card component (BaseCard, BaseStatsCard, BaseDataCard,
BaseSection, BaseToolbar) ships its own styling, so pages never render bare text on the
background.